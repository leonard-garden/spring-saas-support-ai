---
phase: 03-auth-pages
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/lib/authApi.ts
  - frontend/src/components/auth/LogoutButton.tsx
  - frontend/src/pages/ForgotPasswordPage.tsx
  - frontend/src/pages/ResetPasswordPage.tsx
  - frontend/src/pages/DashboardPage.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [AUTH-06, AUTH-07, AUTH-08]
must_haves:
  truths:
    - "User can click Log out and is returned to /login with the session cleared"
    - "User can submit an email on the forgot-password page and see a confirmation message"
    - "User can open the reset-password link with a token and set a new password"
    - "A successful reset shows confirmation and links the user back to /login"
  artifacts:
    - path: "frontend/src/components/auth/LogoutButton.tsx"
      provides: "Logout button calling POST /auth/logout then clearAuth + navigate"
      contains: "/auth/logout"
    - path: "frontend/src/pages/ForgotPasswordPage.tsx"
      provides: "Forgot-password request form"
      min_lines: 30
    - path: "frontend/src/pages/ResetPasswordPage.tsx"
      provides: "Reset-password form reading token from query string"
      min_lines: 35
  key_links:
    - from: "frontend/src/components/auth/LogoutButton.tsx"
      to: "frontend/src/lib/authApi.ts"
      via: "logout() call, then useAuthStore.clearAuth + navigate('/login')"
      pattern: "logout\\("
    - from: "frontend/src/pages/ResetPasswordPage.tsx"
      to: "frontend/src/lib/authApi.ts"
      via: "resetPassword(token, password) on submit"
      pattern: "resetPassword\\("
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/ResetPasswordPage.tsx"
      via: "GuestRoute wrapped /reset-password route"
      pattern: "/reset-password"
---

<objective>
Build the logout action, forgot-password request flow, and reset-password flow.

Purpose: Completes account recovery and session termination (AUTH-06, AUTH-07, AUTH-08).
Output: `LogoutButton` component, `/forgot-password` and `/reset-password` pages, extended `authApi.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/lib/api.ts
@frontend/src/lib/authApi.ts
@frontend/src/store/authStore.ts
@frontend/src/App.tsx

<interfaces>
From frontend/src/store/authStore.ts:
```typescript
useAuthStore: { status, user, accessToken, setAuth(token, user): void, clearAuth(): void }
```

From frontend/src/lib/authApi.ts (created in plan 03-01 — extended here):
```typescript
export function login(email: string, password: string): Promise<{ token: string; user: AuthUser }>
export function signup(businessName: string, email: string, password: string): Promise<AuthResponse>
```

From frontend/src/lib/api.ts:
```typescript
// api axios instance, baseURL = VITE_API_URL (includes /api/v1)
export const api: AxiosInstance
```
</interfaces>

Backend auth endpoints (base path /api/v1 already in api baseURL):
- POST /auth/logout          → no body needed (uses Bearer token) → revokes server-side
- POST /auth/forgot-password → body { email } → always 200 (no account enumeration)
- POST /auth/reset-password  → body { token, newPassword } → 200 on success, 400 on invalid/expired token
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend authApi with logout, forgotPassword, resetPassword</name>
  <files>frontend/src/lib/authApi.ts</files>
  <read_first>
    - frontend/src/lib/authApi.ts (existing login/signup — append, do not rewrite)
    - frontend/src/lib/api.ts (api instance)
    - frontend/src/lib/tokenStorage.ts (clearTokens)
  </read_first>
  <action>
    Append three exported functions to `frontend/src/lib/authApi.ts` (keep existing `login`/`signup` untouched):
    - `logout(): Promise<void>` — `POST /auth/logout` with no body (Bearer token attached by api interceptor). Wrap the call in try/finally is NOT needed here — let the page handle clearAuth. Just `await api.post("/auth/logout")`. Do not throw on non-network errors — but DO allow propagation; the caller will clearAuth regardless.
    - `forgotPassword(email: string): Promise<void>` — `POST /auth/forgot-password` with body `{ email }`.
    - `resetPassword(token: string, newPassword: string): Promise<void>` — `POST /auth/reset-password` with body `{ token, newPassword }`.
    Add named request interfaces `ForgotPasswordRequest { email: string }` and `ResetPasswordRequest { token: string; newPassword: string }`. Explicit return types on all.
  </action>
  <verify>
    <automated>cd frontend && grep -q "/auth/logout" src/lib/authApi.ts && grep -q "/auth/forgot-password" src/lib/authApi.ts && grep -q "/auth/reset-password" src/lib/authApi.ts && grep -q "export function login" src/lib/authApi.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function logout" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "export function forgotPassword" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "export function resetPassword" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "/auth/forgot-password" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "/auth/reset-password" frontend/src/lib/authApi.ts` succeeds
    - existing `login` and `signup` exports still present (`grep -q "export function login"`)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>authApi.ts exposes logout, forgotPassword, resetPassword alongside the existing login/signup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build LogoutButton and wire into DashboardPage</name>
  <files>frontend/src/components/auth/LogoutButton.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <read_first>
    - frontend/src/lib/authApi.ts (logout from Task 1)
    - frontend/src/store/authStore.ts (clearAuth)
    - frontend/src/components/ui/button.tsx
    - frontend/src/pages/DashboardPage.tsx (current placeholder)
    - frontend/src/components/auth/ProtectedRoute.tsx (existing component patterns)
  </read_first>
  <action>
    Create `frontend/src/components/auth/LogoutButton.tsx` exporting `LogoutButton`. It renders a shadcn `Button` (variant="outline") with text "Log out". On click: a `handleLogout` async function that calls `await logout()` inside try/catch (swallow the error — server revocation failure must not block client logout), then in a `finally` block calls `useAuthStore.getState().clearAuth()` and `navigate("/login")` (use `useNavigate`). Disable the button while the request is in flight via a `useState<boolean>` `loading` flag.

    Update `frontend/src/pages/DashboardPage.tsx`: replace the placeholder with a div containing a heading "Dashboard" and the `<LogoutButton />`. Keep it minimal — Phase 4 builds the real shell.
  </action>
  <verify>
    <automated>cd frontend && grep -q "logout(" src/components/auth/LogoutButton.tsx && grep -q "clearAuth" src/components/auth/LogoutButton.tsx && grep -q "LogoutButton" src/pages/DashboardPage.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/auth/LogoutButton.tsx` exports `LogoutButton`
    - `grep -q "navigate(\"/login\")" frontend/src/components/auth/LogoutButton.tsx` succeeds
    - `grep -q "clearAuth" frontend/src/components/auth/LogoutButton.tsx` succeeds
    - `grep -q "finally" frontend/src/components/auth/LogoutButton.tsx` succeeds
    - `grep -q "LogoutButton" frontend/src/pages/DashboardPage.tsx` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard renders a Log out button that revokes the session server-side and returns to /login with state cleared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build ForgotPasswordPage and ResetPasswordPage, wire routes</name>
  <files>frontend/src/pages/ForgotPasswordPage.tsx, frontend/src/pages/ResetPasswordPage.tsx, frontend/src/pages/LoginPage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (form + zod patterns from plan 03-01 to mirror)
    - frontend/src/lib/authApi.ts (forgotPassword, resetPassword from Task 1)
    - frontend/src/components/ui/form.tsx, card.tsx, button.tsx, input.tsx
    - frontend/src/App.tsx (current route wiring)
  </read_first>
  <action>
    Create `frontend/src/pages/ForgotPasswordPage.tsx`. Card-centered form using `react-hook-form` + `zodResolver`. Schema `forgotSchema = z.object({ email: z.string().email("Enter a valid email") })`. One `email` `FormField`, submit `Button` text "Send reset link". On submit: call `forgotPassword(values.email)` in try/catch; on success (or any non-network outcome) set local `useState` `submitted` true and render `<p role="status">If an account exists for that email, a reset link has been sent.</p>`. On network error render `<p role="alert">`. Add `<Link to="/login">` "Back to login".

    Create `frontend/src/pages/ResetPasswordPage.tsx`. Read the `token` query param via `useSearchParams` from react-router-dom: `const [params] = useSearchParams(); const token = params.get("token")`. If `token` is null/empty, render an error card `<p role="alert">This reset link is invalid or has expired.</p>` with a `<Link to="/forgot-password">` "Request a new link" and do not show the form. Otherwise render a form with `zodResolver` schema `resetSchema = z.object({ newPassword: z.string().min(8, "Password must be at least 8 characters"), confirmPassword: z.string() }).refine((d) => d.newPassword === d.confirmPassword, { message: "Passwords do not match", path: ["confirmPassword"] })`. Fields `newPassword` and `confirmPassword`, submit `Button` text "Set new password". On submit: call `resetPassword(token, values.newPassword)` in try/catch; on success set local `useState` `done` true and render `<p role="status">Your password has been reset.</p>` plus `<Link to="/login">` "Go to login"; on error render `<p role="alert">` with the message "This reset link is invalid or has expired. Please request a new one.".

    Update `frontend/src/App.tsx`: import `ForgotPasswordPage` and `ResetPasswordPage`, add routes `<Route path="/forgot-password" element={<GuestRoute><ForgotPasswordPage /></GuestRoute>} />` and `<Route path="/reset-password" element={<GuestRoute><ResetPasswordPage /></GuestRoute>} />` after the `/signup` route.

    Update `frontend/src/pages/LoginPage.tsx`: add a `<Link to="/forgot-password">` with text "Forgot password?" near the password field / submit button.
  </action>
  <verify>
    <automated>cd frontend && grep -q "forgotPassword(" src/pages/ForgotPasswordPage.tsx && grep -q "useSearchParams" src/pages/ResetPasswordPage.tsx && grep -q "resetPassword(" src/pages/ResetPasswordPage.tsx && grep -q "/forgot-password" src/App.tsx && grep -q "/reset-password" src/App.tsx && grep -q "/forgot-password" src/pages/LoginPage.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "forgotSchema" frontend/src/pages/ForgotPasswordPage.tsx` succeeds
    - `grep -q "role=\"status\"" frontend/src/pages/ForgotPasswordPage.tsx` succeeds
    - `grep -q "useSearchParams" frontend/src/pages/ResetPasswordPage.tsx` succeeds
    - `grep -q "params.get(\"token\")" frontend/src/pages/ResetPasswordPage.tsx` succeeds
    - `grep -q "resetSchema" frontend/src/pages/ResetPasswordPage.tsx` succeeds
    - `grep -q "Passwords do not match" frontend/src/pages/ResetPasswordPage.tsx` succeeds
    - `grep -q "GuestRoute><ForgotPasswordPage" frontend/src/App.tsx` succeeds
    - `grep -q "GuestRoute><ResetPasswordPage" frontend/src/App.tsx` succeeds
    - `grep -q "/forgot-password" frontend/src/pages/LoginPage.tsx` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>/forgot-password and /reset-password render validated flows; reset reads token from query string and rejects missing tokens; login links to forgot-password.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- Clicking Log out on the dashboard calls POST /auth/logout, clears the store, and navigates to /login
- /forgot-password submits an email and shows a neutral confirmation
- /reset-password with a `?token=` param shows the reset form; without a token shows an invalid-link error
- Submitting a valid new password calls POST /auth/reset-password and shows a success confirmation
</verification>

<success_criteria>
- AUTH-06: User can log out — server-side revocation plus client state clear
- AUTH-07: User can request a password reset via email
- AUTH-08: User can reset password using the token from the email link
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-pages/03-02-SUMMARY.md`
</output>
