---
phase: 03-auth-pages
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/components/ui/alert.tsx
  - frontend/src/components/auth/EmailVerificationBanner.tsx
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements: [AUTH-09]
must_haves:
  truths:
    - "An authenticated user whose account is unverified sees a visible email verification banner on the dashboard"
    - "An authenticated user whose account IS verified sees no banner"
  artifacts:
    - path: "frontend/src/components/ui/alert.tsx"
      provides: "shadcn Alert primitives"
      contains: "AlertTitle"
    - path: "frontend/src/components/auth/EmailVerificationBanner.tsx"
      provides: "Banner that renders only when user.emailVerified is false"
      contains: "emailVerified"
  key_links:
    - from: "frontend/src/components/auth/EmailVerificationBanner.tsx"
      to: "frontend/src/store/authStore.ts"
      via: "useAuthStore selector reading user.emailVerified"
      pattern: "emailVerified"
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/components/auth/EmailVerificationBanner.tsx"
      via: "rendered at top of dashboard"
      pattern: "EmailVerificationBanner"
---

<objective>
Add an email verification banner that surfaces on the dashboard when the logged-in account is unverified.

Purpose: Completes AUTH-09 — the app correctly surfaces email verification state.
Output: shadcn `alert` component and an `EmailVerificationBanner` rendered on the dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/store/authStore.ts
@frontend/src/types/auth.ts
@frontend/src/pages/DashboardPage.tsx

<interfaces>
From frontend/src/types/auth.ts:
```typescript
export interface AuthUser { id: string; email: string; role: Role; businessId: string; businessName: string; emailVerified: boolean }
```

From frontend/src/store/authStore.ts:
```typescript
useAuthStore: { status, user: AuthUser | null, accessToken, setAuth, clearAuth }
// usage: const user = useAuthStore((s) => s.user)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn alert component</name>
  <files>frontend/src/components/ui/alert.tsx</files>
  <read_first>
    - frontend/src/components/ui/card.tsx (confirm shadcn import style + cn util path)
    - frontend/src/lib/utils.ts (confirm cn export)
  </read_first>
  <action>
    Add the shadcn `alert` component. Run `npx shadcn@2.3.0 add alert` from `frontend/`. If the CLI is blocked (per STATE.md note about npx @ version hook), manually create `frontend/src/components/ui/alert.tsx` with the standard shadcn alert primitives: `Alert` (a `div` with `role="alert"` and a `cva` variants object supporting `default` and `destructive` variants), `AlertTitle` (an `h5`), and `AlertDescription` (a `div`). Use `@/lib/utils` `cn` for class merging and `class-variance-authority` `cva` for variants. Export `Alert`, `AlertTitle`, `AlertDescription`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "AlertTitle" src/components/ui/alert.tsx && grep -q "AlertDescription" src/components/ui/alert.tsx && grep -q "role=\"alert\"" src/components/ui/alert.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/alert.tsx` exists and exports `Alert`, `AlertTitle`, `AlertDescription`
    - `grep -q "role=\"alert\"" frontend/src/components/ui/alert.tsx` succeeds
    - `grep -q "cva" frontend/src/components/ui/alert.tsx` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>shadcn Alert primitives available for import from @/components/ui/alert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build EmailVerificationBanner and render on dashboard</name>
  <files>frontend/src/components/auth/EmailVerificationBanner.tsx, frontend/src/components/auth/EmailVerificationBanner.test.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <read_first>
    - frontend/src/components/ui/alert.tsx (from Task 1)
    - frontend/src/store/authStore.ts (useAuthStore, initialAuthState)
    - frontend/src/types/auth.ts (AuthUser)
    - frontend/src/components/auth/ProtectedRoute.test.tsx (existing test patterns — vitest + store reset)
    - frontend/src/pages/DashboardPage.tsx (current content from plan 03-02)
  </read_first>
  <behavior>
    - When `useAuthStore` has a `user` with `emailVerified: false` → banner renders and contains the text "verify your email"
    - When `user.emailVerified` is `true` → component renders nothing (returns null)
    - When `user` is `null` → component renders nothing (returns null)
  </behavior>
  <action>
    Create `frontend/src/components/auth/EmailVerificationBanner.tsx` exporting `EmailVerificationBanner`. Read the user via `const user = useAuthStore((s) => s.user)`. If `!user || user.emailVerified` return `null`. Otherwise render an `Alert` (variant="destructive" or "default" — use "default" with warning styling) containing `AlertTitle` text "Verify your email address" and `AlertDescription` text "Please check your inbox and verify your email to unlock all features." Ensure the visible copy includes the lowercase phrase "verify your email" so the test assertion matches (the AlertDescription text satisfies this).

    Create `frontend/src/components/auth/EmailVerificationBanner.test.tsx` — a vitest test file mirroring the pattern in `ProtectedRoute.test.tsx`. In `beforeEach`, reset the store with `useAuthStore.setState(initialAuthState)`. Three tests: (1) sets store user with `emailVerified: false` via `useAuthStore.setState({ status: "authenticated", user: {...mockUser, emailVerified: false} })`, renders `<EmailVerificationBanner />`, asserts `screen.getByText(/verify your email/i)` is in the document; (2) sets user with `emailVerified: true`, asserts `screen.queryByText(/verify your email/i)` is null; (3) leaves user null, asserts `container.firstChild` is null. Build a `mockUser: AuthUser` constant with all required fields.

    Update `frontend/src/pages/DashboardPage.tsx`: import `EmailVerificationBanner` and render `<EmailVerificationBanner />` as the first child inside the dashboard container, above the heading and `LogoutButton`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "emailVerified" src/components/auth/EmailVerificationBanner.tsx && grep -q "EmailVerificationBanner" src/pages/DashboardPage.tsx && npx vitest run src/components/auth/EmailVerificationBanner.test.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/auth/EmailVerificationBanner.tsx` exports `EmailVerificationBanner`
    - `grep -q "user.emailVerified" frontend/src/components/auth/EmailVerificationBanner.tsx` succeeds
    - `grep -q "return null" frontend/src/components/auth/EmailVerificationBanner.tsx` succeeds
    - `grep -q "useAuthStore" frontend/src/components/auth/EmailVerificationBanner.tsx` succeeds
    - `frontend/src/components/auth/EmailVerificationBanner.test.tsx` exists with 3 test cases
    - `grep -q "EmailVerificationBanner" frontend/src/pages/DashboardPage.tsx` succeeds
    - `cd frontend && npx vitest run src/components/auth/EmailVerificationBanner.test.tsx` exits 0 (all tests pass)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>An unverified account sees a visible email verification banner on the dashboard; verified accounts and logged-out state render no banner; behavior covered by passing tests.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `npx vitest run src/components/auth/EmailVerificationBanner.test.tsx` passes all 3 cases
- Logging in with an unverified account shows the banner on /dashboard
- Logging in with a verified account shows no banner
</verification>

<success_criteria>
- AUTH-09: Email verification banner shown if account is unverified
- Banner reads emailVerified from the auth store — no extra API call
- Verified and logged-out states correctly render nothing
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-pages/03-03-SUMMARY.md`
</output>
