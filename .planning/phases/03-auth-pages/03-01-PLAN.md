---
phase: 03-auth-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/form.tsx
  - frontend/src/lib/authApi.ts
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/SignupPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "User can submit the login form with email/password and is taken to /dashboard"
    - "User can submit the signup form with business name, email, and password and sees a success confirmation"
    - "Invalid form input shows inline validation errors before any network call"
    - "A failed login/signup API call shows a visible error message"
  artifacts:
    - path: "frontend/src/lib/authApi.ts"
      provides: "login() and signup() API functions hitting /auth/login and /auth/signup"
      contains: "/auth/login"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Login form with react-hook-form + zod"
      min_lines: 40
    - path: "frontend/src/pages/SignupPage.tsx"
      provides: "Signup form with react-hook-form + zod"
      min_lines: 40
    - path: "frontend/src/components/ui/form.tsx"
      provides: "shadcn Form primitives"
      contains: "FormField"
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "frontend/src/lib/authApi.ts"
      via: "login() call on submit, then useAuthStore.setAuth + navigate('/dashboard')"
      pattern: "login\\("
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/SignupPage.tsx"
      via: "GuestRoute wrapped /signup route"
      pattern: "/signup"
---

<objective>
Build the Login and Signup pages with react-hook-form + zod validation, wired to the backend auth endpoints.

Purpose: Completes the entry point of the auth journey (AUTH-01, AUTH-02) — users can create an account and log in.
Output: Functional `/login` and `/signup` pages, a shared `authApi.ts` module, and the shadcn `form` component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/lib/api.ts
@frontend/src/store/authStore.ts
@frontend/src/types/auth.ts
@frontend/src/App.tsx

<interfaces>
From frontend/src/types/auth.ts:
```typescript
export interface AuthUser { id: string; email: string; role: Role; businessId: string; businessName: string; emailVerified: boolean }
export interface AuthResponse { accessToken: string; refreshToken: string; businessId: string; memberId: string }
export type MeResponse = AuthUser
export interface ApiResponse<T> { success: boolean; data: T | null; error: string | null }
```

From frontend/src/store/authStore.ts:
```typescript
useAuthStore: { status, user, accessToken, setAuth(token, user): void, clearAuth(): void }
```

From frontend/src/lib/api.ts:
```typescript
// api is an axios instance, baseURL = VITE_API_URL (already includes /api/v1)
// so api.post("/auth/login", ...) hits {host}/api/v1/auth/login
export const api: AxiosInstance
```

From frontend/src/lib/tokenStorage.ts:
```typescript
setAccessToken(token: string): void
setRefreshToken(token: string): void
```
</interfaces>

Backend auth endpoints (base path /api/v1 already in api baseURL):
- POST /auth/login  → body { email, password }  → returns AuthResponse + user (see note in task 2)
- POST /auth/signup → body { businessName, email, password } → returns AuthResponse
- GET  /auth/me     → returns MeResponse (AuthUser)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn form component</name>
  <files>frontend/src/components/ui/form.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (confirm shadcn import style + cn util path)
    - frontend/src/components/ui/input.tsx
    - frontend/src/components/ui/label.tsx
    - frontend/src/lib/utils.ts (confirm cn export)
  </read_first>
  <action>
    Add the shadcn `form` component. Run `npx shadcn@2.3.0 add form` from `frontend/`. If the CLI is blocked (per STATE.md note about npx @ version hook), manually create `frontend/src/components/ui/form.tsx` with the standard shadcn form primitives that wrap react-hook-form: `Form` (re-export of `FormProvider`), `FormField` (wraps `Controller`), `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage`, and `useFormField` hook. Use `@/lib/utils` `cn` for class merging and `@/components/ui/label` `Label` for `FormLabel`. Ensure `react-hook-form` is in `frontend/package.json` dependencies; if missing run `npm install react-hook-form @hookform/resolvers zod` from `frontend/`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "FormField" src/components/ui/form.tsx && grep -q "react-hook-form" package.json && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/form.tsx` exists and exports `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormMessage`
    - `grep -q "react-hook-form" frontend/package.json` succeeds
    - `grep -q "@hookform/resolvers" frontend/package.json` succeeds
    - `grep -q "\"zod\"" frontend/package.json` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>shadcn form primitives available for import from @/components/ui/form, react-hook-form + zod + resolvers installed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create authApi module</name>
  <files>frontend/src/lib/authApi.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (use the exported `api` instance; baseURL already has /api/v1)
    - frontend/src/lib/tokenStorage.ts (setAccessToken, setRefreshToken)
    - frontend/src/types/auth.ts (AuthResponse, MeResponse, AuthUser)
  </read_first>
  <action>
    Create `frontend/src/lib/authApi.ts` exporting typed functions using the `api` axios instance:
    - `login(email: string, password: string): Promise<{ token: string; user: AuthUser }>` — `POST /auth/login` with body `{ email, password }`. Response is `AuthResponse`. After getting `accessToken` + `refreshToken`, call `setAccessToken(accessToken)` and `setRefreshToken(refreshToken)`, then `GET /auth/me` with the new token to fetch the full `AuthUser`. Return `{ token: accessToken, user }`.
    - `signup(businessName: string, email: string, password: string): Promise<AuthResponse>` — `POST /auth/signup` with body `{ businessName, email, password }`. Return the `AuthResponse` data.
    Define request body types as named interfaces: `LoginRequest { email: string; password: string }`, `SignupRequest { businessName: string; email: string; password: string }`. Do NOT catch errors here — let axios errors propagate so pages can render them. Add explicit return types on all exports.
  </action>
  <verify>
    <automated>cd frontend && grep -q "/auth/login" src/lib/authApi.ts && grep -q "/auth/signup" src/lib/authApi.ts && grep -q "/auth/me" src/lib/authApi.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/authApi.ts` exports `login` and `signup` functions
    - `grep -q "api.post(\"/auth/login\"" frontend/src/lib/authApi.ts` or equivalent `/auth/login` literal present
    - `grep -q "/auth/signup" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "/auth/me" frontend/src/lib/authApi.ts` succeeds
    - `grep -q "setRefreshToken" frontend/src/lib/authApi.ts` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>authApi.ts provides login() and signup() that call the correct endpoints and persist tokens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build LoginPage and SignupPage, wire routes</name>
  <files>frontend/src/pages/LoginPage.tsx, frontend/src/pages/SignupPage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (current placeholder)
    - frontend/src/pages/CorsTestPage.tsx (existing page styling patterns + Card usage)
    - frontend/src/components/ui/form.tsx (from Task 1)
    - frontend/src/components/ui/card.tsx, button.tsx, input.tsx
    - frontend/src/lib/authApi.ts (from Task 2)
    - frontend/src/store/authStore.ts (setAuth)
    - frontend/src/App.tsx (current route wiring, GuestRoute usage)
  </read_first>
  <action>
    Replace the `LoginPage` placeholder. Build a card-centered login form using `react-hook-form` with `zodResolver`. Zod schema `loginSchema = z.object({ email: z.string().email("Enter a valid email"), password: z.string().min(1, "Password is required") })`. Render `Form` / `FormField` / `FormItem` / `FormLabel` / `FormControl` (wrapping `Input`) / `FormMessage` for `email` and `password` fields, plus a submit `Button` with text "Log in". On submit: call `login(values.email, values.password)`, on success call `useAuthStore.getState().setAuth(token, user)` and `navigate("/dashboard")` (use `useNavigate`). On failure, store error in local `useState<string | null>` and render it in a `<p>` with role="alert" and red text. Add a link `<Link to="/signup">` with text "Don't have an account? Sign up".

    Create `frontend/src/pages/SignupPage.tsx` similarly. Zod schema `signupSchema = z.object({ businessName: z.string().min(2, "Business name is required"), email: z.string().email("Enter a valid email"), password: z.string().min(8, "Password must be at least 8 characters") })`. Fields: `businessName`, `email`, `password`. Submit `Button` text "Create account". On submit: call `signup(values.businessName, values.email, values.password)`. On success: set local `useState` `success` boolean true and render a confirmation message `<p role="status">Account created. Please check your email to verify your account, then log in.</p>` plus a `<Link to="/login">` "Go to login". On failure: render error `<p role="alert">`. Add link `<Link to="/login">` "Already have an account? Log in".

    Update `frontend/src/App.tsx`: add `import { SignupPage } from "@/pages/SignupPage"` and add route `<Route path="/signup" element={<GuestRoute><SignupPage /></GuestRoute>} />` directly after the `/login` route.
  </action>
  <verify>
    <automated>cd frontend && grep -q "zodResolver" src/pages/LoginPage.tsx && grep -q "zodResolver" src/pages/SignupPage.tsx && grep -q "/signup" src/App.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "loginSchema" frontend/src/pages/LoginPage.tsx` succeeds
    - `grep -q "login(" frontend/src/pages/LoginPage.tsx` succeeds
    - `grep -q "navigate(\"/dashboard\")" frontend/src/pages/LoginPage.tsx` succeeds
    - `grep -q "setAuth" frontend/src/pages/LoginPage.tsx` succeeds
    - `grep -q "signupSchema" frontend/src/pages/SignupPage.tsx` succeeds
    - `grep -q "businessName" frontend/src/pages/SignupPage.tsx` succeeds
    - `grep -q "role=\"alert\"" frontend/src/pages/LoginPage.tsx` and `frontend/src/pages/SignupPage.tsx` both succeed
    - `grep -q "GuestRoute><SignupPage" frontend/src/App.tsx` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>/login and /signup render validated forms; login navigates to dashboard on success; signup shows a confirmation; both show API errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `/login` route renders an email/password form with inline zod validation
- `/signup` route renders a businessName/email/password form with inline zod validation
- Submitting valid login credentials calls POST /auth/login then GET /auth/me and navigates to /dashboard
- Submitting valid signup calls POST /auth/signup and shows a success confirmation
</verification>

<success_criteria>
- AUTH-01: User can log in with email and password and land on the dashboard
- AUTH-02: User can sign up with business name, email, and password and receive a confirmation
- All forms validate input client-side before any network call
- API failures surface a visible error message
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-pages/03-01-SUMMARY.md`
</output>
