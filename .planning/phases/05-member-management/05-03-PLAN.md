---
phase: 05-member-management
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/components/members/RemoveConfirmDialog.tsx
  - frontend/src/components/members/RemoveConfirmDialog.test.tsx
  - frontend/src/components/members/MembersTable.tsx
  - frontend/src/components/members/MembersTable.test.tsx
  - frontend/src/pages/MembersPage.tsx
  - frontend/src/pages/MembersPage.test.tsx
autonomous: true
requirements: [MBR-03, MBR-04, MBR-05]
must_haves:
  truths:
    - "An OWNER sees a per-row actions control with Remove and Change Role options"
    - "Clicking Remove opens a confirmation dialog naming the member; confirming calls the remove API"
    - "Changing a role via the dropdown calls the role-change API and refreshes the table"
    - "A MEMBER role user sees no Remove, Change Role, or Invite controls anywhere on the page"
    - "The Remove control is hidden on the OWNER's own row (self-removal is blocked by backend)"
  artifacts:
    - path: "frontend/src/components/members/RemoveConfirmDialog.tsx"
      provides: "Confirmation dialog that calls removeMember on confirm"
      min_lines: 35
  key_links:
    - from: "frontend/src/components/members/RemoveConfirmDialog.tsx"
      to: "frontend/src/lib/memberApi.ts"
      via: "useMutation with mutationFn: removeMember"
      pattern: "removeMember"
    - from: "frontend/src/components/members/MembersTable.tsx"
      to: "frontend/src/lib/memberApi.ts"
      via: "changeRole called from role dropdown"
      pattern: "changeRole"
    - from: "frontend/src/pages/MembersPage.tsx"
      to: "frontend/src/components/members/RemoveConfirmDialog.tsx"
      via: "controlled by removeTarget state"
      pattern: "RemoveConfirmDialog"
---

<objective>
Complete Phase 5: per-row Remove and Change Role actions for OWNER users, a confirmation dialog for removal, and full enforcement that MEMBER-role users see no mutation controls.

Purpose: Delivers MBR-03 (remove with confirmation), MBR-04 (change role), and completes MBR-05 (ADMIN-only — gated to OWNER per RESEARCH discovery 3 — controls hidden from non-OWNER). Mutations gate to OWNER because the backend enforces `requireOwner()` on DELETE and PATCH role.
Output: `RemoveConfirmDialog.tsx` + test, extended `MembersTable.tsx` + test, extended `MembersPage.tsx` + test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-member-management/05-RESEARCH.md
@.planning/phases/05-member-management/05-01-SUMMARY.md
@.planning/phases/05-member-management/05-02-SUMMARY.md

<interfaces>
<!-- Contracts from plans 05-01 / 05-02 — build against these. -->

From frontend/src/lib/memberApi.ts:
```typescript
export async function removeMember(id: string): Promise<void>          // DELETE — 204, no body
export async function changeRole(id: string, req: UpdateRoleRequest): Promise<MemberResponse>
```

From frontend/src/types/member.ts:
```typescript
export interface MemberResponse { id: string; email: string; role: string; createdAt: string }
export interface UpdateRoleRequest { role: "ADMIN" | "MEMBER" }  // cannot assign OWNER
```

From frontend/src/components/members/MembersTable.tsx (built in 05-01):
Props interface currently `{ members: MemberResponse[] }` — this plan EXTENDS it.

From frontend/src/pages/MembersPage.tsx (after 05-02):
Has `user`, `canMutate = user?.role === "OWNER"`, `inviteOpen` state, renders `MembersTable` and `InviteModal`.

shadcn dropdown-menu (added in 05-01) exports: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem.
shadcn dialog (added in 05-01) exports: Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter.

RESEARCH Pitfall 5: hide the Remove control on the row where `member.id === user.id` (backend throws SelfModificationException).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RemoveConfirmDialog with removeMember mutation</name>
  <files>frontend/src/components/members/RemoveConfirmDialog.tsx, frontend/src/components/members/RemoveConfirmDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/members/InviteModal.tsx (the Dialog + useMutation + invalidateQueries pattern to mirror — built in 05-02)
    - frontend/src/components/ui/dialog.tsx (Dialog primitive exports)
    - frontend/src/lib/memberApi.ts (removeMember signature)
    - frontend/src/types/member.ts (MemberResponse shape)
    - .planning/phases/05-member-management/05-RESEARCH.md (sections "Pattern 2", "Pitfall 3", "Pitfall 5")
  </read_first>
  <behavior>
    - Test: when given a target member, the dialog shows the member's email in the confirmation text
    - Test: clicking Confirm calls removeMember with the target id
    - Test: clicking Cancel calls onClose and does NOT call removeMember
    - Test: on successful remove, onClose is called (dialog closes)
  </behavior>
  <action>
    Create `frontend/src/components/members/RemoveConfirmDialog.tsx`:
    - Props interface: `{ target: MemberResponse | null; onClose: () => void }`.
    - The dialog is `open` when `target !== null`; `onOpenChange` → call `onClose`.
    - `const queryClient = useQueryClient()`.
    - Mutation:
      ```typescript
      const removeMutation = useMutation({
        mutationFn: (id: string) => removeMember(id),
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ["members"] })
          onClose()
        },
      })
      ```
    - Render shadcn `Dialog` → `DialogContent` → `DialogHeader` with title "Remove member" and description `Are you sure you want to remove ${target?.email}? This action cannot be undone.` (guard with `target &&`).
    - `DialogFooter` with a Cancel button (`onClose`) and a destructive-variant Confirm button that calls `removeMutation.mutate(target!.id)`; disabled while `removeMutation.isPending`.
    - On mutation error, show the server error message in the dialog.

    Create `frontend/src/components/members/RemoveConfirmDialog.test.tsx`:
    - `vi.mock("@/lib/memberApi", ...)` with `removeMember` as a `vi.fn()` resolving to undefined.
    - Wrap in `QueryClientProvider` (fresh client, retry false).
    - Pass a `vi.fn()` `onClose`.
    - Use a sample `target` MemberResponse.
    - Cover the four behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/members/RemoveConfirmDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/members/RemoveConfirmDialog.tsx` exists
    - `grep 'removeMember' frontend/src/components/members/RemoveConfirmDialog.tsx` matches
    - `grep 'invalidateQueries' frontend/src/components/members/RemoveConfirmDialog.tsx` matches with `["members"]`
    - `grep -E 'target.*email' frontend/src/components/members/RemoveConfirmDialog.tsx` matches (member named in text)
    - `cd frontend && npm test -- --run src/components/members/RemoveConfirmDialog.test.tsx` — all tests pass
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RemoveConfirmDialog names the target member, calls removeMember on confirm, invalidates the members query, closes on success or cancel; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-row actions (Remove + Change Role) to MembersTable</name>
  <files>frontend/src/components/members/MembersTable.tsx, frontend/src/components/members/MembersTable.test.tsx</files>
  <read_first>
    - frontend/src/components/members/MembersTable.tsx (current version from 05-01 — props `{ members }`, ROLE_LABEL map, columns)
    - frontend/src/components/ui/dropdown-menu.tsx (DropdownMenu primitive exports)
    - frontend/src/lib/memberApi.ts (changeRole signature)
    - .planning/phases/05-member-management/05-RESEARCH.md (sections "Pattern 2", "Pattern 3", "Pitfall 5", "Pitfall 6")
  </read_first>
  <behavior>
    - Test: when canMutate is true, each row (except the current user's own row) shows an actions trigger
    - Test: when canMutate is false, no actions trigger renders on any row
    - Test: choosing a role from the dropdown calls changeRole with the member id and { role }
    - Test: clicking Remove in the dropdown calls onRemove with that member
    - Test: the current user's own row does NOT show a Remove option
  </behavior>
  <action>
    Edit `frontend/src/components/members/MembersTable.tsx`:
    - Extend the props interface to:
      ```typescript
      interface MembersTableProps {
        members: MemberResponse[]
        canMutate: boolean
        currentUserId: string | undefined
        onRemove: (member: MemberResponse) => void
      }
      ```
    - Add an "Actions" column header — render its cells only when `canMutate`. When `!canMutate`, do not render the Actions column at all (header + cells).
    - In each row's Actions cell render a shadcn `DropdownMenu`:
      - `DropdownMenuTrigger` is a small ghost `Button` with a lucide `MoreHorizontal` icon.
      - `DropdownMenuContent` with:
        - Two "Change role to ..." items — one for each role NOT equal to the member's current role, limited to ADMIN and MEMBER (never OWNER). Each item calls a local `handleRoleChange(member.id, newRole)`.
        - A "Remove" item — only rendered when `member.id !== currentUserId` (RESEARCH Pitfall 5) — calls `onRemove(member)`.
    - Role change uses an internal `useMutation`:
      ```typescript
      const queryClient = useQueryClient()
      const roleMutation = useMutation({
        mutationFn: ({ id, role }: { id: string; role: "ADMIN" | "MEMBER" }) => changeRole(id, { role }),
        onSuccess: () => queryClient.invalidateQueries({ queryKey: ["members"] }),
      })
      ```
      `handleRoleChange` calls `roleMutation.mutate({ id, role })`.

    Create `frontend/src/components/members/MembersTable.test.tsx`:
    - `vi.mock("@/lib/memberApi", ...)` with `changeRole` as a `vi.fn()` resolving to a MemberResponse.
    - Wrap in `QueryClientProvider` (fresh client, retry false).
    - Render with a small `members` array including one row whose `id` equals `currentUserId`.
    - Cover the five behaviors above. Use `user-event` to open the dropdown and click items.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/members/MembersTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'changeRole' frontend/src/components/members/MembersTable.tsx` matches
    - `grep -E 'currentUserId' frontend/src/components/members/MembersTable.tsx` matches and is used to gate the Remove item
    - `grep 'canMutate' frontend/src/components/members/MembersTable.tsx` matches and gates the Actions column
    - `grep 'invalidateQueries' frontend/src/components/members/MembersTable.tsx` matches with `["members"]`
    - MembersTable.test.tsx has a test asserting the current user's own row shows no Remove option
    - `cd frontend && npm test -- --run src/components/members/MembersTable.test.tsx` — all tests pass
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MembersTable shows a per-row actions dropdown for OWNER users only, with role-change items (calling changeRole) and a Remove item hidden on the user's own row; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire RemoveConfirmDialog + updated MembersTable props into MembersPage</name>
  <files>frontend/src/pages/MembersPage.tsx, frontend/src/pages/MembersPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/MembersPage.tsx (current version after 05-02 — has user, canMutate, inviteOpen, renders MembersTable + InviteModal)
    - frontend/src/pages/MembersPage.test.tsx (current tests after 05-02 — extend, do not break)
    - frontend/src/components/members/RemoveConfirmDialog.tsx (Task 1 — props { target, onClose })
    - frontend/src/components/members/MembersTable.tsx (Task 2 — new props canMutate, currentUserId, onRemove)
    - .planning/phases/05-member-management/05-RESEARCH.md (section "MembersPage skeleton")
  </read_first>
  <action>
    Edit `frontend/src/pages/MembersPage.tsx`:
    - Add `const [removeTarget, setRemoveTarget] = useState<MemberResponse | null>(null)`.
    - Pass new props to `<MembersTable>`: `canMutate={canMutate}`, `currentUserId={user?.id}`, `onRemove={setRemoveTarget}` (in addition to existing `members={pageItems}`).
    - Render `<RemoveConfirmDialog target={removeTarget} onClose={() => setRemoveTarget(null)} />` once at page level (alongside `<InviteModal />`).

    Edit `frontend/src/pages/MembersPage.test.tsx` — add tests (keep all existing passing):
    - "MEMBER role user sees no actions controls" — set user role "MEMBER", render with members present, assert no actions trigger / no Remove / no Change role text appears (covers MBR-05 end-to-end).
    - "OWNER clicking Remove on a row opens the confirm dialog" — render as OWNER with members, open a row's actions, click Remove, assert the confirm dialog text (`/remove member/i` or the email) appears.
    Update the existing `vi.mock("@/lib/memberApi", ...)` to also stub `removeMember` and `changeRole` as `vi.fn()`s if not already present.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/pages/MembersPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'removeTarget|setRemoveTarget' frontend/src/pages/MembersPage.tsx` matches
    - `grep 'RemoveConfirmDialog' frontend/src/pages/MembersPage.tsx` matches (imported + rendered)
    - `grep -E 'currentUserId=\{user\?\.id\}' frontend/src/pages/MembersPage.tsx` matches
    - `grep -E 'onRemove=' frontend/src/pages/MembersPage.tsx` matches
    - MembersPage.test.tsx has a test proving MEMBER role sees no Remove/Change role/Invite controls
    - `cd frontend && npm test -- --run src/pages/MembersPage.test.tsx` — all tests pass (all prior + new)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MembersPage wires the Remove confirm dialog and passes mutation props to MembersTable; MEMBER role users see zero mutation controls; tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm test -- --run` — full Phase 5 suite green (MembersPage, InviteModal, RemoveConfirmDialog, MembersTable)
- Manual as OWNER: open a row's actions → change a role (table refreshes), click Remove → confirm dialog names the member → confirm removes them; own row has no Remove
- Manual as MEMBER: members table visible, but no Invite button, no row actions
</verification>

<success_criteria>
- MBR-03: OWNER can remove a member after confirming a dialog
- MBR-04: OWNER can change a member's role via an inline dropdown action
- MBR-05: MEMBER role users see no invite, remove, or role-change controls anywhere
- Own-row Remove is hidden (avoids backend SelfModificationException)
- All mutations invalidate the `["members"]` query so the table stays fresh
</success_criteria>

<output>
After completion, create `.planning/phases/05-member-management/05-03-SUMMARY.md`
</output>
