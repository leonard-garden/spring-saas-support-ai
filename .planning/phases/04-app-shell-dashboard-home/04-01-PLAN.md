---
phase: 04-app-shell-dashboard-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/SidebarNavLink.tsx
  - frontend/src/components/layout/Sidebar.test.tsx
  - frontend/src/pages/MembersPage.tsx
  - frontend/src/pages/KbPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]
must_haves:
  truths:
    - "Every authenticated page renders inside a persistent sidebar (sidebar does not remount on navigation)"
    - "Sidebar shows Home, Members, and Knowledge Base nav links"
    - "Sidebar displays the logged-in user's name (email), role, and business name"
    - "The active route's nav link has a visually distinct highlight class"
    - "Navigating to /members and /kb resolves without a 404"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Layout component: sidebar + <Outlet />"
      contains: "Outlet"
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Nav links, user info display, logout button"
      contains: "useAuthStore"
    - path: "frontend/src/components/layout/SidebarNavLink.tsx"
      provides: "Single nav item with active highlight via NavLink"
      contains: "NavLink"
    - path: "frontend/src/components/layout/Sidebar.test.tsx"
      provides: "Tests for SHELL-01/02/03"
    - path: "frontend/src/pages/MembersPage.tsx"
      provides: "Stub page so /members nav link resolves"
    - path: "frontend/src/pages/KbPage.tsx"
      provides: "Stub page so /kb nav link resolves"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/layout/AppShell.tsx"
      via: "layout route element wrapping child routes in <Outlet />"
      pattern: "AppShell"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/src/store/authStore.ts"
      via: "useAuthStore selector for user"
      pattern: "useAuthStore"
---

<objective>
Build the persistent sidebar layout shell that wraps all authenticated pages, and convert App.tsx from flat protected routes to a React Router v7 layout route.

Purpose: Every page inside the app needs a consistent sidebar with navigation, user context, and logout. This is the structural foundation Phase 5 (Members) and Phase 6 (KB) build on top of.
Output: AppShell, Sidebar, SidebarNavLink components; MembersPage/KbPage stubs; updated App.tsx route structure; Sidebar.test.tsx covering SHELL-01/02/03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-app-shell-dashboard-home/04-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

From frontend/src/types/auth.ts:
```ts
export type Role = "OWNER" | "ADMIN" | "AGENT"
export interface AuthUser {
  id: string
  email: string
  role: Role
  businessId: string
  businessName: string
  emailVerified: boolean
}
```

From frontend/src/store/authStore.ts:
```ts
// useAuthStore((s) => s.user) returns AuthUser | null
// useAuthStore((s) => s.status) returns "loading" | "authenticated" | "unauthenticated"
export const useAuthStore = create<AuthState>()(...)
export const initialAuthState = { status: "loading", user: null, accessToken: null }
```

From frontend/src/components/auth/LogoutButton.tsx:
```tsx
export function LogoutButton() // renders <Button variant="outline">Log out</Button>, calls logout() + clearAuth() + navigate("/login")
```

Current frontend/src/App.tsx route structure (flat — to be replaced):
```tsx
<Route path="/dashboard" element={<ProtectedRoute><DashboardPage /></ProtectedRoute>} />
```

Test setup: frontend/vitest.config.ts (jsdom, globals), frontend/src/test/setup.ts (cleanup + localStorage.clear afterEach).
Test command: `cd frontend && npm test -- --run`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Sidebar tests + build SidebarNavLink and Sidebar components</name>
  <files>frontend/src/components/layout/SidebarNavLink.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/Sidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/auth/EmailVerificationBanner.test.tsx (test pattern: beforeEach resets useAuthStore.setState(initialAuthState), uses baseUser AuthUser fixture)
    - frontend/src/store/authStore.ts (store shape, initialAuthState export)
    - frontend/src/types/auth.ts (AuthUser, Role types)
    - frontend/src/components/auth/LogoutButton.tsx (LogoutButton component to embed in Sidebar)
    - frontend/src/components/ui/button.tsx (Button variants available)
  </read_first>
  <behavior>
    - Test 1 (SHELL-01): Sidebar renders 3 nav links with text "Home", "Members", "Knowledge Base"
    - Test 2 (SHELL-01): Home link has href="/dashboard", Members link href="/members", Knowledge Base link href="/kb"
    - Test 3 (SHELL-02): Sidebar renders user.email, user.role, and user.businessName when store has an authenticated user
    - Test 4 (SHELL-03): when rendered at route "/members", the Members NavLink has the active class "bg-primary" and the Home NavLink does not
    - Test 5: Sidebar renders a "Log out" button
  </behavior>
  <action>
    Create `SidebarNavLink.tsx`: a component with props `{ to: string; icon: ReactNode; label: string; end?: boolean }`. It renders a react-router-dom `NavLink` with `to={to}` and `end={end}`. The `className` is a callback `({ isActive }) => ...` producing: base classes `"flex items-center gap-3 px-3 py-2 rounded-md text-sm font-medium transition-colors"`, and when `isActive` append `"bg-primary text-primary-foreground"`, else append `"text-muted-foreground hover:bg-accent hover:text-accent-foreground"`. Render `{icon}` then `<span>{label}</span>` inside.

    Create `Sidebar.tsx`: renders an `<aside className="w-64 shrink-0 border-r bg-card flex flex-col h-full">`. Top section: app name `<div className="px-4 py-4 text-lg font-semibold border-b">Support AI</div>`. Nav section: `<nav className="flex-1 px-2 py-4 space-y-1">` containing three `SidebarNavLink` instances:
      - `<SidebarNavLink to="/dashboard" end icon={<Home className="h-4 w-4" />} label="Home" />`
      - `<SidebarNavLink to="/members" icon={<Users className="h-4 w-4" />} label="Members" />`
      - `<SidebarNavLink to="/kb" icon={<BookOpen className="h-4 w-4" />} label="Knowledge Base" />`
    Import `Home, Users, BookOpen` from `"lucide-react"`. The `end` prop on the Home link prevents it staying active on /members and /kb (NavLink uses prefix matching by default).
    Bottom user-info section: `const user = useAuthStore((s) => s.user)`. Render a `<div className="border-t px-4 py-3 space-y-1">` containing `<p className="text-sm font-medium">{user?.email}</p>`, `<p className="text-xs text-muted-foreground">{user?.role} · {user?.businessName}</p>`, and `<div className="pt-2"><LogoutButton /></div>`. Import `LogoutButton` from `"@/components/auth/LogoutButton"` and `useAuthStore` from `"@/store/authStore"`.

    Create `Sidebar.test.tsx`: follow the EmailVerificationBanner.test.tsx pattern exactly — import `{ render, screen }` from `"@testing-library/react"`, `{ beforeEach, describe, expect, it }` from `"vitest"`, `{ useAuthStore, initialAuthState }` from `"@/store/authStore"`, `{ MemoryRouter }` from `"react-router-dom"`, and an `AuthUser` fixture `baseUser` with `{ id: "u1", email: "owner@acme.com", role: "OWNER", businessId: "b1", businessName: "Acme Inc", emailVerified: true }`. In `beforeEach`, `useAuthStore.setState({ ...initialAuthState, status: "authenticated", user: baseUser })`. Helper `renderAt(path: string)` renders `<MemoryRouter initialEntries={[path]}><Sidebar /></MemoryRouter>`. Write the 5 behavior tests above. For SHELL-03, query the link by role/name and assert `.className` contains `"bg-primary"`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/layout/Sidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/layout/SidebarNavLink.tsx` contains `NavLink` and `isActive`
    - `frontend/src/components/layout/Sidebar.tsx` contains `useAuthStore`, `LogoutButton`, `to="/dashboard"`, `to="/members"`, `to="/kb"`, and `end`
    - `frontend/src/components/layout/Sidebar.tsx` contains `Home`, `Users`, `BookOpen` imported from `lucide-react`
    - `frontend/src/components/layout/Sidebar.test.tsx` contains at least 5 `it(` blocks
    - `cd frontend && npm test -- --run src/components/layout/Sidebar.test.tsx` exits 0
  </acceptance_criteria>
  <done>Sidebar renders 3 nav links, user name/role/business name, a logout button, and highlights the active route link. All 5 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build AppShell, stub pages, and convert App.tsx to a layout route</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/pages/MembersPage.tsx, frontend/src/pages/KbPage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current flat route structure that must be replaced)
    - frontend/src/components/layout/Sidebar.tsx (created in Task 1 — AppShell imports it)
    - frontend/src/components/auth/ProtectedRoute.tsx (wraps the layout route element)
    - frontend/src/pages/DashboardPage.tsx (existing page rendered in the outlet)
  </read_first>
  <action>
    Create `AppShell.tsx`: import `{ Outlet }` from `"react-router-dom"` and `{ Sidebar }` from `"./Sidebar"`. Export `function AppShell()` returning:
    ```tsx
    <div className="flex h-screen">
      <Sidebar />
      <main className="flex-1 overflow-y-auto p-6"><Outlet /></main>
    </div>
    ```

    Create `MembersPage.tsx`: export `function MembersPage()` returning `<h1 className="text-2xl font-semibold">Members</h1>`. This is a Phase 4 stub — Phase 5 rebuilds it fully. Do not add any API calls.

    Create `KbPage.tsx`: export `function KbPage()` returning `<h1 className="text-2xl font-semibold">Knowledge Base</h1>`. This is a Phase 4 stub — Phase 6 rebuilds it fully. Do not add any API calls.

    Update `App.tsx`: add imports `import { AppShell } from "@/components/layout/AppShell"`, `import { MembersPage } from "@/pages/MembersPage"`, `import { KbPage } from "@/pages/KbPage"`. Replace the single line `<Route path="/dashboard" element={<ProtectedRoute><DashboardPage /></ProtectedRoute>} />` with a layout route block:
    ```tsx
    <Route element={<ProtectedRoute><AppShell /></ProtectedRoute>}>
      <Route path="/dashboard" element={<DashboardPage />} />
      <Route path="/members" element={<MembersPage />} />
      <Route path="/kb" element={<KbPage />} />
    </Route>
    ```
    Leave all other routes (`/cors-test`, `/login`, `/signup`, `/forgot-password`, `/reset-password`, `*`) unchanged. Do NOT wrap the child routes in their own `ProtectedRoute` — the layout route wraps once and children inherit protection.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/layout/AppShell.tsx` contains `Outlet` and `<Sidebar />`
    - `frontend/src/pages/MembersPage.tsx` exports `MembersPage` and contains no `api` or `fetch` call
    - `frontend/src/pages/KbPage.tsx` exports `KbPage` and contains no `api` or `fetch` call
    - `frontend/src/App.tsx` contains `<ProtectedRoute><AppShell /></ProtectedRoute>` and `path="/members"` and `path="/kb"`
    - `frontend/src/App.tsx` no longer contains `<ProtectedRoute><DashboardPage /></ProtectedRoute>`
    - `cd frontend && npm test -- --run` exits 0
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>AppShell renders Sidebar + Outlet; /dashboard, /members, /kb all resolve inside the shell behind a single ProtectedRoute; full test suite and typecheck pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run` — full suite green
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — no type errors
- `cd frontend && npm run dev` then visit /dashboard, /members, /kb — sidebar persists, active link highlights, no 404
</verification>

<success_criteria>
- SHELL-01: Sidebar with Home, Members, Knowledge Base links renders on every authenticated page
- SHELL-02: Sidebar shows user email, role, business name from the auth store
- SHELL-03: Active route's nav link has the bg-primary highlight class
- Sidebar mounts once via layout route (no remount flicker on navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/04-app-shell-dashboard-home/04-01-SUMMARY.md`
</output>
