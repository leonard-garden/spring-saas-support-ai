---
phase: 01-scaffold-cors-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/components.json
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/.env.development
  - frontend/.env.development.example
  - frontend/.gitignore
  - frontend/index.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "frontend/ directory exists at repo root with Vite + React 18 + TypeScript project"
    - "All locked stack libraries are installed in frontend/package.json"
    - "Tailwind CSS v3 + shadcn/ui (CLI 2.3.0) is wired with classic @tailwind directives"
    - "Path alias @/* resolves in both tsconfig.json and tsconfig.app.json"
    - "VITE_API_URL env var loads from .env.development and is consumed by axios instance"
    - "npm run build exits 0 with no TypeScript errors"
  artifacts:
    - path: "frontend/package.json"
      provides: "Locked stack: vite, react, react-dom, typescript, react-router-dom, @tanstack/react-query, axios, zustand, react-hook-form, @hookform/resolvers, zod, lucide-react, tailwindcss@3, postcss, autoprefixer, tailwindcss-animate, class-variance-authority, clsx, tailwind-merge"
      contains: '"tailwindcss": "^3'
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind v3 config with shadcn theme tokens + Plus Jakarta Sans font family"
      contains: "darkMode"
    - path: "frontend/src/index.css"
      provides: "Tailwind v3 directives + shadcn CSS variables + Plus Jakarta Sans @import"
      contains: "@tailwind base"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() helper from shadcn init"
      contains: "export function cn"
    - path: "frontend/src/lib/api.ts"
      provides: "axios instance pre-configured for backend baseURL"
      contains: "import.meta.env.VITE_API_URL"
    - path: "frontend/components.json"
      provides: "shadcn/ui config — Tailwind v3 baseline"
      contains: '"tailwind"'
    - path: "frontend/.env.development"
      provides: "VITE_API_URL pointing at local backend"
      contains: "VITE_API_URL=http://localhost:8081/api/v1"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "frontend/.env.development"
      via: "import.meta.env.VITE_API_URL at module load"
      pattern: "import.meta.env.VITE_API_URL"
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/index.css"
      via: "CSS variables consumed via hsl(var(--xxx)) in theme.extend.colors"
      pattern: "hsl\\(var\\(--"
    - from: "frontend/tsconfig.app.json"
      to: "frontend/src/**"
      via: "@/* path alias resolution for shadcn imports"
      pattern: '"@/\\*"'
---

<objective>
Create the `frontend/` Vite + React 18 + TypeScript project at the repo root with the full locked stack installed (axios, react-router-dom, zustand, react-query, react-hook-form, zod, shadcn/ui on Tailwind v3). Wire path aliases, env-driven axios base URL, design-system tokens (Plus Jakarta Sans, violet primary, zinc neutrals), and prove the toolchain compiles via `npm run build`.

Purpose: This is the infrastructure gate for the entire v0.2 milestone. Plan 01-02 cannot verify CORS without this scaffolding. Phases 2–7 cannot start without this scaffolding.

Output: A buildable `frontend/` project — no user-facing UI yet, but every library plan 01-02 and downstream phases need is installed, configured, and importable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-cors-verification/01-CONTEXT.md
@.planning/phases/01-scaffold-cors-verification/01-RESEARCH.md
@.planning/phases/01-scaffold-cors-verification/DESIGN-SYSTEM.md
@.planning/phases/01-scaffold-cors-verification/API-CONTRACT.md
@src/main/resources/application-dev.yml
@src/main/resources/application.yml

<interfaces>
<!-- Backend contracts the frontend must align with. Verified against Java source. -->

Backend dev server: http://localhost:8081
Backend API base path: /api/v1
Allowed CORS origins (dev profile, application-dev.yml):
  - http://localhost:3000
  - http://localhost:5173   <-- Vite default port (pre-configured)
Backend response envelope:
  { success: boolean, data: T | null, error: string | null }
Backend error format (4xx/5xx): RFC 7807 ProblemDetail (NOT the envelope)
Auth flow: Bearer tokens in Authorization header (NOT cookies)
  → axios withCredentials MUST be false

POST /api/v1/auth/login expects:
  { email: string, password: string }
Returns 200 with:
  { success: true,
    data: { accessToken, refreshToken, businessId, memberId },
    error: null }

</interfaces>

<design_tokens>
<!-- From DESIGN-SYSTEM.md — must be wired in this plan, even though no pages use them yet. -->

Font: Plus Jakarta Sans (Google Fonts, weights 400/500/600/700)
Primary brand: violet-600 (#7C3AED)
Neutral scale: zinc-50 through zinc-950
Sidebar background: zinc-950 (#09090B)
shadcn CSS variables (light mode):
  --background: 0 0% 98%
  --foreground: 240 3.7% 10.9%
  --primary: 262 83.3% 57.8%
  --primary-foreground: 0 0% 100%
  --radius: 0.5rem
  (plus card, popover, secondary, muted, accent, destructive, border, input, ring)
</design_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install full locked stack</name>

  <read_first>
    - .planning/phases/01-scaffold-cors-verification/01-RESEARCH.md (Standard Stack + Installation section — exact version pins and install ordering)
    - .planning/phases/01-scaffold-cors-verification/01-CONTEXT.md (D-01 through D-14 — locked decisions)
    - CLAUDE.md (project root — confirms repo layout: pom.xml + src/ already at root, frontend/ is a sibling)
  </read_first>

  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/.env.development
    frontend/.env.development.example
    frontend/.gitignore
  </files>

  <action>
    Create the `frontend/` directory at the repo root (sibling to `pom.xml` and `src/`).

    From the repo root, run these commands in order:

    ```bash
    # 1. Scaffold Vite + React + TypeScript template
    npm create vite@latest frontend -- --template react-ts

    # 2. Enter project, install base deps
    cd frontend
    npm install

    # 3. Install runtime stack (per D-08..D-13)
    npm install react-router-dom@^7 \
      axios@^1 \
      @tanstack/react-query@^5 \
      zustand@^5 \
      react-hook-form@^7 \
      @hookform/resolvers@^3 \
      zod@^3 \
      lucide-react@^0.400.0

    # 4. Install Tailwind v3 (MUST pin to v3 — never v4) and PostCSS peers
    npm install -D tailwindcss@^3 postcss autoprefixer @types/node
    ```

    Do NOT run `npx shadcn` here — Task 2 handles that.

    After installs, replace the scaffolded `frontend/vite.config.ts` with EXACTLY this content (pinned port 5173 + strictPort so we fail fast if 5173 is taken, + @/ alias):

    ```typescript
    import path from "path"
    import react from "@vitejs/plugin-react"
    import { defineConfig } from "vite"

    export default defineConfig({
      plugins: [react()],
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "./src"),
        },
      },
      server: {
        port: 5173,
        strictPort: true,
      },
    })
    ```

    Patch `frontend/tsconfig.json` so the `compilerOptions` block contains `baseUrl` and `paths` for the `@/*` alias (Vite scaffolds this file with only `references` — you must add a `compilerOptions` block). Final shape:

    ```json
    {
      "files": [],
      "references": [
        { "path": "./tsconfig.app.json" },
        { "path": "./tsconfig.node.json" }
      ],
      "compilerOptions": {
        "baseUrl": ".",
        "paths": { "@/*": ["./src/*"] }
      }
    }
    ```

    Patch `frontend/tsconfig.app.json` `compilerOptions` to ALSO include `baseUrl` and `paths` (Pitfall 2 — both files need the alias, otherwise IDE/build mismatch). Keep all existing strict options and add:

    ```jsonc
    {
      "compilerOptions": {
        // ...existing options preserved (strict, jsx, target, etc.)...
        "baseUrl": ".",
        "paths": { "@/*": ["./src/*"] }
      }
    }
    ```

    Create `frontend/.env.development` with this exact content:

    ```
    VITE_API_URL=http://localhost:8081/api/v1
    ```

    Create `frontend/.env.development.example` (this one IS committed) with the same content as `.env.development`.

    Create or append to `frontend/.gitignore` to ignore real env files but keep the example:

    ```
    .env.development
    .env.production
    .env.local
    !.env.development.example
    ```

    Reasoning notes embedded in commits/file comments:
    - Per D-14, Tailwind v3 only (avoid Pitfall 1 from RESEARCH.md).
    - Per D-03, npm only (no pnpm/yarn/bun).
    - Per D-02, directory name is `frontend/` (NOT `admin-ui/`). The ROADMAP.md mention of `admin-ui/` is stale; CONTEXT.md D-02 overrides it.
    - strictPort: true prevents silent port shift to 5174 which would trip Pitfall 5 (backend only allows 5173 in CORS).
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/` directory exists at repo root (sibling to `pom.xml`)
    - `frontend/package.json` exists and `node -p "require('./frontend/package.json').dependencies.react"` outputs a string starting with `^18` or `18`
    - `frontend/package.json` `dependencies` contains all of: `react`, `react-dom`, `react-router-dom`, `axios`, `@tanstack/react-query`, `zustand`, `react-hook-form`, `@hookform/resolvers`, `zod`, `lucide-react`
    - `frontend/package.json` `devDependencies` contains all of: `vite`, `typescript`, `tailwindcss`, `postcss`, `autoprefixer`, `@types/node`
    - `grep -E '"tailwindcss":\s*"\^?3' frontend/package.json` matches (Tailwind v3 pinned, not v4)
    - `grep '"@": path.resolve' frontend/vite.config.ts` matches
    - `grep 'strictPort: true' frontend/vite.config.ts` matches
    - `grep 'port: 5173' frontend/vite.config.ts` matches
    - `grep '"@/\*"' frontend/tsconfig.json` matches
    - `grep '"@/\*"' frontend/tsconfig.app.json` matches
    - `frontend/.env.development` contains exact line `VITE_API_URL=http://localhost:8081/api/v1`
    - `frontend/.env.development.example` exists with the same VITE_API_URL line
    - `grep '^.env.development$' frontend/.gitignore` matches
    - `cd frontend && npm run build` exits 0 with no TypeScript errors and produces `frontend/dist/`
  </acceptance_criteria>

  <done>
    Vite + React 18 + TypeScript project compiles. Every locked stack library is installed at the version range from RESEARCH.md. Path alias `@/*` resolves in both tsconfig files. `VITE_API_URL` is wired and committed example exists. Tailwind v3 is installed but not yet configured (Task 2 finishes that).
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui (CLI 2.3.0), add base components, write design tokens + axios instance</name>

  <read_first>
    - .planning/phases/01-scaffold-cors-verification/01-RESEARCH.md (Patterns 1-7; Pitfall 1 — MUST use shadcn@2.3.0, never shadcn@latest)
    - .planning/phases/01-scaffold-cors-verification/DESIGN-SYSTEM.md (Plus Jakarta Sans, violet/zinc palette, shadcn CSS variables block)
    - frontend/tsconfig.json (must already have @/* alias from Task 1 before shadcn init runs)
    - frontend/tsconfig.app.json (must already have @/* alias from Task 1 before shadcn init runs)
    - frontend/vite.config.ts (must already have @ alias from Task 1)
    - frontend/.env.development (so api.ts knows the env var name)
  </read_first>

  <files>
    frontend/tailwind.config.js
    frontend/postcss.config.js
    frontend/components.json
    frontend/src/index.css
    frontend/src/lib/utils.ts
    frontend/src/lib/api.ts
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/label.tsx
    frontend/src/components/ui/card.tsx
    frontend/index.html
  </files>

  <action>
    All commands run from `frontend/`.

    Step 1 — Generate Tailwind v3 config + postcss config:

    ```bash
    cd frontend
    npx tailwindcss@3 init -p
    ```

    This creates `tailwind.config.js` and `postcss.config.js`. Confirm `postcss.config.js` has both `tailwindcss: {}` and `autoprefixer: {}` plugins. If it does not, overwrite it with:

    ```javascript
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }
    ```

    Step 2 — Run shadcn CLI 2.3.0 (PINNED VERSION, NOT @latest — Pitfall 1):

    ```bash
    npx shadcn@2.3.0 init
    ```

    When prompted, answer:
    - Style: default
    - Base color: neutral (zinc family)
    - CSS variables: yes
    - tsx: yes
    - tailwind.config: tailwind.config.js
    - css file: src/index.css
    - aliases: components=@/components, utils=@/lib/utils, ui=@/components/ui, lib=@/lib, hooks=@/hooks
    - icon library: lucide

    If the CLI runs non-interactively, write `components.json` directly with this exact content:

    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "default",
      "rsc": false,
      "tsx": true,
      "tailwind": {
        "config": "tailwind.config.js",
        "css": "src/index.css",
        "baseColor": "neutral",
        "cssVariables": true,
        "prefix": ""
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    Step 3 — Add the four base components Plan 01-02 needs:

    ```bash
    npx shadcn@2.3.0 add button input label card
    ```

    This populates `frontend/src/components/ui/{button,input,label,card}.tsx` and `frontend/src/lib/utils.ts` (with the `cn()` helper). It also adds `class-variance-authority`, `clsx`, `tailwind-merge`, `tailwindcss-animate`, and `@radix-ui/react-label`, `@radix-ui/react-slot` to package.json.

    Step 4 — Overwrite `frontend/tailwind.config.js` with the final design-system-aware config (extend shadcn defaults with Plus Jakarta Sans + violet brand colors per DESIGN-SYSTEM.md):

    ```javascript
    /** @type {import('tailwindcss').Config} */
    export default {
      darkMode: ["class"],
      content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
      theme: {
        extend: {
          fontFamily: {
            sans: ["Plus Jakarta Sans", "system-ui", "sans-serif"],
          },
          borderRadius: {
            lg: "var(--radius)",
            md: "calc(var(--radius) - 2px)",
            sm: "calc(var(--radius) - 4px)",
          },
          colors: {
            background: "hsl(var(--background))",
            foreground: "hsl(var(--foreground))",
            card: {
              DEFAULT: "hsl(var(--card))",
              foreground: "hsl(var(--card-foreground))",
            },
            popover: {
              DEFAULT: "hsl(var(--popover))",
              foreground: "hsl(var(--popover-foreground))",
            },
            primary: {
              DEFAULT: "hsl(var(--primary))",
              foreground: "hsl(var(--primary-foreground))",
            },
            secondary: {
              DEFAULT: "hsl(var(--secondary))",
              foreground: "hsl(var(--secondary-foreground))",
            },
            muted: {
              DEFAULT: "hsl(var(--muted))",
              foreground: "hsl(var(--muted-foreground))",
            },
            accent: {
              DEFAULT: "hsl(var(--accent))",
              foreground: "hsl(var(--accent-foreground))",
            },
            destructive: {
              DEFAULT: "hsl(var(--destructive))",
              foreground: "hsl(var(--destructive-foreground))",
            },
            border: "hsl(var(--border))",
            input: "hsl(var(--input))",
            ring: "hsl(var(--ring))",
            brand: {
              50: "#F5F3FF",
              100: "#EDE9FE",
              500: "#8B5CF6",
              600: "#7C3AED",
              700: "#6D28D9",
              900: "#4C1D95",
            },
          },
        },
      },
      plugins: [require("tailwindcss-animate")],
    }
    ```

    Step 5 — Overwrite `frontend/src/index.css` with classic Tailwind v3 directives + Plus Jakarta Sans + shadcn CSS variables (per DESIGN-SYSTEM.md). DO NOT use `@import "tailwindcss"` — that's the v4 pattern (Pitfall 1).

    ```css
    @import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap");

    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      :root {
        --background: 0 0% 98%;
        --foreground: 240 3.7% 10.9%;
        --card: 0 0% 100%;
        --card-foreground: 240 3.7% 10.9%;
        --popover: 0 0% 100%;
        --popover-foreground: 240 3.7% 10.9%;
        --primary: 262 83.3% 57.8%;
        --primary-foreground: 0 0% 100%;
        --secondary: 240 4.8% 95.9%;
        --secondary-foreground: 240 5.9% 10%;
        --muted: 240 4.8% 95.9%;
        --muted-foreground: 240 3.8% 46.1%;
        --accent: 240 4.8% 95.9%;
        --accent-foreground: 240 5.9% 10%;
        --destructive: 0 84.2% 60.2%;
        --destructive-foreground: 0 0% 98%;
        --border: 240 5.9% 90%;
        --input: 240 5.9% 90%;
        --ring: 262 83.3% 57.8%;
        --radius: 0.5rem;
      }

      * {
        @apply border-border;
      }
      body {
        @apply bg-background text-foreground font-sans antialiased;
      }
    }
    ```

    Step 6 — Update `frontend/index.html` `<title>` to `Support SaaS Admin` (replace the default "Vite + React + TS" title). The default Vite scaffold puts the title between `<title>` tags inside `<head>`.

    Step 7 — Create `frontend/src/lib/api.ts` (minimal axios instance, no interceptors — Phase 2 will add them):

    ```typescript
    import axios from "axios"

    const baseURL = import.meta.env.VITE_API_URL

    if (!baseURL) {
      // Fail loud at module load if env var missing — saves debugging time
      // eslint-disable-next-line no-console
      console.error(
        "VITE_API_URL is not defined. Copy frontend/.env.development.example to frontend/.env.development."
      )
    }

    export const api = axios.create({
      baseURL,
      headers: { "Content-Type": "application/json" },
      // withCredentials intentionally false — backend uses Bearer tokens, not cookies.
      // Setting true requires the backend to whitelist exact origin (which it does), but
      // adds preflight complexity that the Bearer flow does not need.
      withCredentials: false,
    })

    export default api
    ```

    Step 8 — Verify `frontend/src/lib/utils.ts` exists with the `cn()` helper (shadcn init should have written it). If shadcn skipped it for any reason, write:

    ```typescript
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    Step 9 — Run `npm run build` to confirm everything compiles.

    Why these choices:
    - shadcn@2.3.0 is the LAST CLI version that targets Tailwind v3. `shadcn@latest` (v4.x) generates `@import "tailwindcss"` and `@tailwindcss/vite` plugin usage — both incompatible with our v3 stack (RESEARCH.md Pitfall 1).
    - `withCredentials: false` matches backend Bearer token flow. Despite backend `allowCredentials=true`, cookies are unused.
    - Plus Jakarta Sans imported via Google Fonts CSS @import keeps the font wired without npm coupling.
    - The `brand` color scale gives us a direct violet handle in addition to `primary` (HSL var-based). Either can be used downstream.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npm run build &amp;&amp; test -f src/components/ui/button.tsx &amp;&amp; test -f src/components/ui/input.tsx &amp;&amp; test -f src/components/ui/card.tsx &amp;&amp; test -f src/components/ui/label.tsx &amp;&amp; test -f src/lib/utils.ts &amp;&amp; test -f src/lib/api.ts &amp;&amp; test -f components.json</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/components.json` exists and contains `"baseColor": "neutral"` and `"tsx": true`
    - `frontend/tailwind.config.js` exists, contains `darkMode: ["class"]`, contains `"Plus Jakarta Sans"`, contains `tailwindcss-animate`, contains `hsl(var(--primary))`
    - `grep '@tailwind base' frontend/src/index.css` matches (classic v3 directives, not v4 `@import`)
    - `grep -L '@import "tailwindcss"' frontend/src/index.css` succeeds (file must NOT contain that v4 line)
    - `grep 'Plus+Jakarta+Sans' frontend/src/index.css` matches
    - `grep -- '--primary: 262 83.3% 57.8%' frontend/src/index.css` matches
    - `frontend/src/lib/utils.ts` contains `export function cn`
    - `frontend/src/lib/api.ts` exists and contains `import.meta.env.VITE_API_URL` and `withCredentials: false`
    - `frontend/src/components/ui/button.tsx`, `input.tsx`, `label.tsx`, `card.tsx` all exist
    - `frontend/package.json` `dependencies` contains `class-variance-authority`, `clsx`, `tailwind-merge`
    - `frontend/package.json` `dependencies` contains `tailwindcss-animate` (or it appears in devDependencies)
    - `frontend/postcss.config.js` exists and contains both `tailwindcss` and `autoprefixer` keys
    - `cd frontend && npm run build` exits 0 (TypeScript + Vite production build passes)
    - `grep '<title>Support SaaS Admin</title>' frontend/index.html` matches
  </acceptance_criteria>

  <done>
    shadcn/ui (CLI 2.3.0, Tailwind v3-compatible) is wired. Four base components (Button, Input, Label, Card) needed by Plan 01-02 are generated. Design tokens from DESIGN-SYSTEM.md (Plus Jakarta Sans, violet primary, zinc neutrals, shadcn CSS variables) are written to index.css and tailwind.config.js. axios instance is exported from `@/lib/api` and pulls baseURL from VITE_API_URL. `npm run build` passes. Plan 01-02 can now import everything it needs.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from `frontend/`:
1. `npm run build` — exits 0, no TypeScript errors, `dist/` created
2. `cat package.json | grep -E '"(react|axios|zustand|@tanstack/react-query|react-router-dom|react-hook-form|zod|tailwindcss|lucide-react)"'` — every locked library appears
3. `ls src/components/ui/` — shows at minimum `button.tsx input.tsx label.tsx card.tsx`
4. `grep -c '@tailwind' src/index.css` — outputs at least 3 (base + components + utilities)

No browser check yet — Plan 01-02 handles the live CORS verification.
</verification>

<success_criteria>
- `frontend/` directory exists at repo root with Vite + React 18 + TypeScript project
- All stack libraries from D-07..D-14 installed in `frontend/package.json` (Tailwind pinned to v3)
- Path alias `@/*` resolves in both tsconfig files
- shadcn/ui (CLI 2.3.0) initialized — Button, Input, Label, Card components present
- Design tokens wired: Plus Jakarta Sans font, violet primary (--primary 262 83.3% 57.8%), zinc neutrals, shadcn CSS variables
- `frontend/src/lib/api.ts` exports an axios instance bound to `import.meta.env.VITE_API_URL`
- `frontend/.env.development` contains `VITE_API_URL=http://localhost:8081/api/v1`
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-cors-verification/01-01-SUMMARY.md` documenting:
- Exact installed versions (from package.json)
- shadcn CLI version used (must be 2.3.0)
- Any deviation from the plan (e.g., shadcn CLI prompt differences)
- Confirmation that `npm run build` exits 0
</output>
